<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="5dp"
    android:layout_marginHorizontal="20dp"
    android:background="@drawable/scene_entry_bg"
    android:clickable="true"
    android:focusable="true"
    android:paddingVertical="16dp"
    android:paddingHorizontal="20dp">

    <!-- 会议号标签 -->
    <TextView
        android:id="@+id/tv_room_id_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/meeting_id_label"
        android:textColor="#80FFFFFF"
        android:textSize="12sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 会议号 -->
    <TextView
        android:id="@+id/tv_room_id"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/iv_user_count"
        app:layout_constraintTop_toBottomOf="@id/tv_room_id_label"
        tools:text="123456" />

    <!-- 人数图标 -->
    <ImageView
        android:id="@+id/iv_user_count"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginLeft="16dp"
        android:src="@android:drawable/ic_menu_myplaces"
        app:tint="#80FFFFFF"
        app:layout_constraintBottom_toBottomOf="@id/tv_user_count"
        app:layout_constraintRight_toLeftOf="@id/tv_user_count"
        app:layout_constraintTop_toTopOf="@id/tv_user_count" />

    <!-- 当前人数 -->
    <TextView
        android:id="@+id/tv_user_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="6dp"
        android:textColor="#CCFFFFFF"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="@id/tv_room_id"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_room_id"
        tools:text="3 participants" />

</androidx.constraintlayout.widget.ConstraintLayout>
